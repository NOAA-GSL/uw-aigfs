# -*- mode: sh -*-

# Export CONDADIR to specify alternate conda install path.

run=$(readlink -f $(dirname ${BASH_SOURCE[0]}))/run
. $($run condadir)/etc/profile.d/conda.sh
conda activate $($run env)
